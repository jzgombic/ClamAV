pipeline {
  agent any
  stages {
    stage('Install Prerequisites') {
      steps {
        sh("sudo apt update")
      }
    }
    stage('Install Ansible') {
      steps {
        sh("sup apt update")
      }
    }
    stage('Deploy ClamAV') {
      steps {
        sh("sudo apt update")
      }
    }
  }
}
